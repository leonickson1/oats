import Foundation

// The product name, in one place. Internal type names, the bundle id, and the
// on-disk data folders stay "Earshot" so existing notes keep working; only what
// people see says Oats.
enum Brand {
    static let name = "Oats"
    static let tagline = "Meeting notes that never leave your Mac."
}
